@page
@model GerenciamentoTurismo.Pages_Clientes.CreateModel

        @{
        ViewData["Title"] = "Create";
        }
        
        <h1>Create</h1>
        
    <h4>Cliente</h4>
    <hr />
    <div class="row">
    <div class="col-md-4">
    <form method="post">
            @* CHAMANDO A PARTIAL VIEW AQUI *@
            <partial name="~/Pages/Shared/_ClienteFormFieldsPartial.cshtml" model="Model.Cliente" />

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
