@page
@model GerenciamentoTurismo.Pages.LogoutModel
@{
    // Esta página não precisa de conteúdo visual.
    // Sua única finalidade é ser o alvo do formulário de logout
    // e acionar o método OnPostAsync no seu PageModel.
    // Por isso, deixamos o corpo do arquivo vazio.
}
